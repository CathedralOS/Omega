
(def epsilon_evaluate_block ((context EpsilonEvaluatorInvocation)
  (statements EpsilonStatementList) (terminalValue EpsilonOptionalTerminal)
  (bindings EpsilonEvaluatorBindings)
  (store EpsilonRuntimeStore) (output Bytes))
  EpsilonEvaluatorBlockOutcome
  (match (epsilon_evaluate_statements context statements bindings store output)
    ((EpsilonEvaluatorBlockFalloff nextBindings nextStore nextOutput)
      (match terminalValue
        (NoEpsilonTerminal
          (EpsilonEvaluatorBlockFalloff nextBindings nextStore nextOutput))
        ((SomeEpsilonTerminal terminal)
          (epsilon_evaluate_terminal context terminal nextBindings nextStore
            nextOutput))))
    ((EpsilonEvaluatorBlockTransfer stateValue nextBindings nextStore nextOutput)
      (EpsilonEvaluatorBlockTransfer stateValue nextBindings nextStore nextOutput))
    ((EpsilonEvaluatorBlockReturn value nextStore nextOutput)
      (EpsilonEvaluatorBlockReturn value nextStore nextOutput))
    ((EpsilonEvaluatorBlockExit code nextOutput)
      (EpsilonEvaluatorBlockExit code nextOutput))
    ((EpsilonEvaluatorBlockTrap kind nextOutput)
      (EpsilonEvaluatorBlockTrap kind nextOutput))
    ((EpsilonEvaluatorBlockInternal offset) (EpsilonEvaluatorBlockInternal offset))))

(def epsilon_evaluator_stopped_block ((outcome EpsilonEvaluatorOutcome)
  (offset Int)) EpsilonEvaluatorBlockOutcome
  (match outcome
    ((EpsilonEvaluatorValue value place store output)
      (EpsilonEvaluatorBlockInternal offset))
    ((EpsilonEvaluatorResultless store output)
      (EpsilonEvaluatorBlockInternal offset))
    ((EpsilonEvaluatorExit code output) (EpsilonEvaluatorBlockExit code output))
    ((EpsilonEvaluatorTrap kind output) (EpsilonEvaluatorBlockTrap kind output))
    ((EpsilonEvaluatorInternal coordinate)
      (EpsilonEvaluatorBlockInternal coordinate))))
