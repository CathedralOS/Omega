# OMGCOMP refinement witness v15 candidate

Status: private bounded carrier and focused independent reference seam; not yet
lower-rooted persisted-Beta R1--R5 admission.

OMGRFN15 frames the CKIR13 full-width `u32 in Trapping` subtraction milestone.
Its exact 40-byte little-endian header uses magic `OMGRFNF\0`, outer version
15, entry flags 1, exact OMGCOMP/OMGRSW5/CKIR13/ELF extents, claimed result,
and low-byte exit projection.  Components occur in that order and exact EOF,
the inherited component ceilings, and the 4,497,544-byte whole-frame ceiling
remain normative.

The focused positive carrier is the exact direct leaf-minus-leaf producer
fixture.  It assigns maximum and near-maximum full-width values, computes
`4294967295 - 4294967290`, tests the exact result 5, and returns 70.  The
independent frame reference owns exact frame and source custody, OMGRSW5
identity/full canonical `u32 in Trapping`, complete CKIR13 validation and
result 70, exactly one opcode-26 relation, and recognition of the conservative
ELF load/subtract/carry/range/store template.

The companion underflow source is not an admitted successful result carrier:
it lowers to CKIR13, the independent evaluator traps, and the emitted artifact
contains the same borrow-to-trap path.  Treating it as a successful result-70
frame would be unsound.

This candidate intentionally does **not** claim the complete persisted-Beta
R1--R5 closure used by frozen OMGRFN14.  In particular, regex recognition of
the selected ELF instruction template is not byte-complete artifact
reconstruction.  Final OMGRFN15 admission still requires separate persisted
owners for source-to-witness resolution, witness-to-CKIR tables, source
lowering and artifact-free result, complete CKIR meaning, and exact ELF bytes.
The focused gate exists to freeze the correct frame/version/component boundary
without laundering that later assurance work into compiler evidence.
