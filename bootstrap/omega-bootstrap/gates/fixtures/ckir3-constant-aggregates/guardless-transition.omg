// Focused control for the ordinary one-arm guardless transition. CKIR lowers
// this source form as a Jump; it must not synthesize a Boolean guard.

data GuardlessProbe {}

machine GuardlessProbe::run(&mut self) -> u8 {
    transition { _ -> pass() }

    state pass(&mut self) { 70 }
}
