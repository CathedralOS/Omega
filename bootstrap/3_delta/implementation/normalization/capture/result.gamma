; One ordered immutable binding list serves both parameters and call arguments.
(def capture_finish ((count Int) (reversed Int)) Int
  (pair count (syntax_reverse_nodes count reversed 0)))

(def capture_count ((result Int)) Int (first result))
(def capture_bindings ((result Int)) Int (second result))
