use omega::host::contracts::stdout;
use omega::host::targets::windows::kernel32;
use omega::host::targets::windows::types;

capability WindowsStdout {
    kernel32: WindowsKernel32

    entry write_line(text: String) -> Result<(), IOError> where {
        requires text.utf8
        ensures result.Ok || result.Err
        boundary omega_windows_stdout_adapter
        boundary omega_windows_kernel32
    }
}
