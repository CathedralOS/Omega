// package-manager fixture: exact build-owned provider selection.
target windows_x86_64 { }
target linux_x86_64 { }
target linux_arm64 { }
target macos_arm64 { }

machine build(builder: &mut Build) {
    builder.package("provider-switchboard");
    builder.select_provider<ClockHost, MonotonicClock>();
}
