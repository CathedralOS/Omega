// Intentionally over-declared network reach fixture.
pub boundary trait NetworkHost {
    machine send(endpoint: &[u8], bytes: &[u8]);
}

pub machine local_format(value: u64) -> u64
reaches NetworkHost
{
    value
}
