// package-manager fixture root.

machine build(builder: &mut Build) {
    builder.package("graph-workbench");
    builder.depend(Source::Path {
        location: "../arithmetic-kernels"
    });
    builder.depend(Source::Path {
        location: "../file-journal"
    });
}
