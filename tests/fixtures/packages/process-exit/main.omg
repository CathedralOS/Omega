// Public reach of one exact ordinary-package Console boundary.
use host_services::console;

pub machine terminate(console: Console, return_code: i32)
reaches Console
invokes console;
{
    console.exit_process(return_code);
}
