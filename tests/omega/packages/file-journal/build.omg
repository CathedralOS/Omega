// package-manager fixture: no build-host reach; runtime API is capability-bearing.

machine build(builder: &mut Build) {
    builder.package("file_journal");
    builder.depend(Source::Path { location: "../host-services" });
}
