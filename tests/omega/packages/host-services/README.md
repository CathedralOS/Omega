# Host services fixture

This ordinary package supplies the exact Console and FilesystemHost surfaces
used by package-review fixtures. Its deliberately unprivileged name proves
semantic roles are accepted from exact package-owned declarations and schemas,
never inferred from `std`, a repository, or a source path.
