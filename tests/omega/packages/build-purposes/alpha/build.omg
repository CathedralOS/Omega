// package-manager fixture: a host tool library selected only through a
// build edge (wiki/spec/build/scoped_execution.md, dependency purposes).

machine build(builder: &mut Build) {
    builder.package("alpha");
}
