// False twin of proofs/proof_addition_commutativity (ladder rung L4).
// a + b == b + a + 1 normalizes to 0 == 1. Accepted today; term
// normalization (canonical polynomial form) must reject it.

machine false_add_commutative(a: u64, b: u64)
ensures
    a + b == b + a + 1
{
}

data Main {
}

machine Main::main(&mut self) {
}
