machine build(builder: &mut Build) {
    builder.application("wire_compatibility_preservation_unmet");
    builder.require_wire_compatibility<
        RelayChannel,
        MessageLineage,
        LocalMessage,
        PeerMessage,
        PreserveUnknown
    >();
}
