machine build(builder: &mut Build) {
    builder.application("wire-compatibility-preservation-unmet");
    builder.require_wire_compatibility<
        RelayChannel,
        MessageLineage,
        LocalMessage,
        PeerMessage,
        PreserveUnknown
    >();
}
