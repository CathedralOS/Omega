// Chapter 20 (Wire Protocols): decoding produces typed Omega values, so every
// wire field type must resolve to a declared type.

data CounterMessage {
    #0 counter: i32;
    #1 header: MissingHeader;
}

data Main {
}

machine Main::main(&mut self) {
}
