machine build(builder: &mut Build) {
    builder.application("wire_compatibility_migration_route_missing");
    builder.require_wire_compatibility<
        ArchiveStore,
        CounterLineage,
        Counter,
        CounterV1,
        CompleteMigration
    >();
}
