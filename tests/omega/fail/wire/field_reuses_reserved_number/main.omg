// Chapter 20 (Wire Protocols): reserved field numbers prevent accidental
// reuse after a field is retired; declaring a field on a reserved number in
// the same schema scope is rejected.

data CounterMessage {
    #0 counter: i32;
    #3 retry_count: i32;

    retired #3;
}

data Main {
}

machine Main::main(&mut self) {
}
