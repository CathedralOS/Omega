data Entry {
    value: i32;
}

data Main {
    entries: [Entry; 2];
    index: u64;
}

machine Main::main(&mut self) -> i32 {
    self.index = 9;
    let chosen: Entry = self.entries[self.index];
    chosen.value
}
