domain i32::Degrees;

machine + i32::Degrees::add(left: i32, right: i32) -> i32 {
    ((left as i32 in Wrapping) + (right as i32 in Wrapping)) as i32
}

data Main {
}

machine Main::main(&mut self) {
    let degrees: i32 in Degrees = 90 as i32 in Degrees;
    let values: [i32; 1] = [degrees];
}
