domain i64::Km;
domain i64::Miles;

machine choose(select_left: bool, left: i64, right: i64) -> i64 {
    (match select_left {
        true -> left as i64 in Km,
        false -> right as i64 in Miles
    }) as i64
}
