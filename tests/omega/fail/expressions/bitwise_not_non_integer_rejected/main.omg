// Integer complement is spelled `~`; it never reinterprets Boolean storage.
use omega::language::std::console;
data Main { console: Console; }
machine Main::main(&mut self) { let flag: bool = true; let value: bool = ~flag; self.console.exit_process(0); }
