// Payload fields are NAMED (`case Say(value: i32);`); a bare type with no
// field name is malformed and must be a parse error, not silently accepted.

data Command {
    case None;
    case Say(i32);
}

data Main {
    value: i32;
}

machine Main::main(&mut self) {
}
