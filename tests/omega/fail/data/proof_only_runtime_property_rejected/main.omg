// `[copy]` is a claim about runtime values; proof-only data never becomes one.
use omega::language::std::console;
data Nat [copy] { case Zero; case Succ(prev: Nat); }
data Main { console: Console; }
machine Main::main(&mut self) { self.console.exit_process(0); }
