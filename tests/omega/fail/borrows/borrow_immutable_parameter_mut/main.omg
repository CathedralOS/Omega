data Main {
    value: i32;
}

machine Main::main(&mut self) {
    write(self.value);

    state write(input_value: i32) {
        copy(&mut input_value);
    }

    state copy(out_value: &mut i32) {
        out_value = 2;
    }
}
