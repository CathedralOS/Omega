// FAIL canary: `nonblocking` names neither a boundary service nor one of the
// independent operational clauses. Service resolution rejects it.

data Guard {
    handle: i32;
}

machine Guard::drop(&mut self)
reaches
    nonblocking
{
}

data Main {
}

machine Main::main(&mut self) {
    let g: Guard = Guard { handle: 1 };
}
