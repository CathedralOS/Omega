data Quantity {
    value: i32;
}

domain Quantity::Additive
requires
    self.value >= 0;

operator Quantity::Additive::combine<T, U>(left: T, right: U) -> T;
operator Quantity::Additive::combine<A, B>(left: B, right: A) -> B;

data Main {
}

machine Main::main(&mut self) {
}
