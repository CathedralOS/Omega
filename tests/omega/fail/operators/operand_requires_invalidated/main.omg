boundary operator == Integer::equal(left: i32, right: i32) -> bool
requires right >= 0;

machine reset(value: &mut i32) -> i32 {
    value = -1;
    0
}

machine compare(mut value: i32) -> bool
requires value >= 0 {
    reset(&mut value) == value
}
