// Wrapping deliberately has no float-to-integer conversion requirement.
use omega::language::core::float_operations;

data Main { out: i32; }

machine Main::main(&mut self) {
    let wrapped: i32 in Wrapping = I32::from_f64(1.0);
    self.out = wrapped as i32;
}
