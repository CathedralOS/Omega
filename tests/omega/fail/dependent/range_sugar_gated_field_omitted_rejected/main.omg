// FAIL CANARY. The omitted health field would be zero, outside its standing
// range, so range-as-default-domain sugar makes it mandatory.
data Player { health: i32 [1..=100]; score: i32; }
data Main {}
machine Main::main(&mut self) {
    let player: Player = Player {};
}
