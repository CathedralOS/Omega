// A Task<T> is only a lifecycle claim. Compiler/provider-owned parked control
// storage is not a field that ordinary Omega can project from another
// activation's claim.
use omega::language::core::task;

machine inspect(task: &Task<i32>) {
    let leaked: u64 = task.continuation;
}
