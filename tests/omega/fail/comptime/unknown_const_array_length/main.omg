// COMPTIME STAGE 1 -- EVALUATION FAILURE UX: a const call that cannot be
// evaluated is a compile error AT the array-length position naming the
// machine and the failure. `missing_size` does not exist anywhere in the
// program.
use omega::language::std::console;

data Main {
    console: Console;
    slots: [i64; missing_size()];
}

machine Main::main(&mut self) {
    self.console.exit_process(0);
}
