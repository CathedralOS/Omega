data Unit {
    exponent: i32;
}

domain<T, const U: Unit> T::Quantity<U>;

machine wrong(value: i64) -> i64 {
    transition { _ -> (value as i64 in Quantity) }
}

data Main {
}

machine Main::main(&mut self) {
}
