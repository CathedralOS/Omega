target windows_x86_64 {
}

machine build(builder: &mut Build) {
    builder.application("unknown-program-entry-binding");
    builder.roots.bind(windows_x86_64::ProgramEntry, Application::missing);
}
