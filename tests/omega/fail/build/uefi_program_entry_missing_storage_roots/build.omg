
machine build(builder: &mut Build) {
    builder.application("uefi_program_entry_missing_storage_roots");
    builder.subsystem = Subsystem::EfiApplication;
    builder.freestanding = true;
    builder.roots.bind(uefi_x86_64::ProgramEntry, launch);
}
