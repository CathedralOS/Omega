// The failure path performs only a load and therefore cannot publish.

data Main { counter: AtomicU32; }

machine Main::main(&mut self) {
    let prior: u32 in Wrapping =
        self.counter.compare_exchange(0, 1, GlobalOrder, Publish);
}
