data Room {
    x: i32;
}

data Main {
}

machine Main::main(
    &mut self
) {
    let rooms: [Room; 1] = [Room { x: 0 }];
    let index: UInt = 0;

    rooms[index].x = 1;

    transition {
        true -> main()
    }
}
