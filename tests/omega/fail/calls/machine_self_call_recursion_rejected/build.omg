// State-graph rejection requires one exact target-owned entry selection.

machine build(builder: &mut Build) {
    builder.application("machine-self-call-recursion-rejected");
    builder.roots.bind(linux_x86_64::ProgramEntry, Main::main);
}
