// CLI truthfully declares MachineControl reach, but a hosted program does not
// own the machine and therefore cannot discharge the instruction's authority
// requirement merely by listing the service.
use omega::language::core::assembly;

data Main {}

machine Main::main(&mut self) reaches MachineControl {
    asm { cli }
}
