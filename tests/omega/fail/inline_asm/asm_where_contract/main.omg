// Assembly facts are boolean proof obligations. They cannot use an integer
// place as though it were a proposition.

data Main {
    value: i32;
}

machine Main::main(&mut self) {
    asm where
        requires self.value
        clobbers none
    {
        hlt
    }
}
