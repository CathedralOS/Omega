// Ordinary returns are hidden control exits in source assembly. Generated
// code may realize checked machine returns, but a user block must expose every
// edge through Omega control flow.
data Main {}
machine Main::main(&mut self) {
    asm { ret }
}
