// FAIL: `Main::run` is bound as the target ProgramEntry while declaring typed
// arrival parameters. A hosted program entry exposes no arrival parameters, so
// the binding is rejected rather than silently forwarding `handle`/`table`.
data Main { }
machine Main::run(&self, handle: addr, table: addr) -> i32 {
    transition { _ -> (0) }
}
