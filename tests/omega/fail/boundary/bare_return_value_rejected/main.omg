// A machine state return type spelling a bare boundary trait is a value
// position; only `Service<R>` carries a service.
boundary trait Console {
    machine write_line(text: &[u8]);
}

data Main { }

machine Main::main(&mut self) {
    state make(&mut self) -> Console {
        transition { _ -> (0) }
    }
}
