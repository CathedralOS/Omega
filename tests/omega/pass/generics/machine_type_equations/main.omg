// A type argument supplies declared structure; no runtime value supplies layout.
machine capacity<Length, const Capacity: u64>() -> u64
where Length == u64[0..=Capacity]
{ Capacity }

machine recovered() -> u64 { capacity<u64[0..257]>() }
