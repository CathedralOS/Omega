data Main {
    out: i32;
}

machine Main::main(&mut self) {
    let flag: bool = true;
    let picked: i32 = self.pick(flag);
    self.out = picked;
}

machine Main::pick(&mut self, flag: bool) -> i32 {
    transition flag {
        true -> 1
        false -> 2
    }
    
}
