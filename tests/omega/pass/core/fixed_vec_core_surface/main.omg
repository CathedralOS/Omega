use omega::language::core::fixed_vec;

// Confirms the ALLOCATOR STORY STAGE 1 FixedVec surface is browsable end to
// end: importing the core `fixed_vec` module brings in the generic
// fixed-capacity vector data shape (`FixedVec<T, const N: u64>` — inline
// `[T; N]` storage plus a live length, no heap anywhere) and the
// Length / push / pop / get / as_slice boundary contracts with their
// proof-obligated requires/ensures, and the program type-checks cleanly.
//
// The machine-shaped implementation pattern is pinned concretely (i32, N=4)
// in collections/runtime_fixed_vec_round_trip_exit; attaching machines to
// the GENERIC data is blocked on generic machine instantiation (see the
// frontier note inside core/fixed_vec.omg).

data Main {
}

machine Main::main(&mut self) {
}
