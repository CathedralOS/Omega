machine build(builder: &mut Build) {
    builder.application("carry-permission-provider-adapter");
    builder.accept_boundary<ClaimProvider>();
    builder.select_provider<ClaimProvider, ClaimProviderImpl>();
}
