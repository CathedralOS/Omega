// `first` and `second` publish identically named lineage markers and eras.
// The store policy selects `first::Lineage`, `first::Counter`, and
// `first::CounterV1` by their qualified paths; only the route bound on the
// `first` declarations satisfies the CompleteMigration demand. Leaf names and
// declaration order cannot dispatch: `second`'s shapes carry no route for
// this lineage and must not be selected by position or spelling.

use first;
use second;

data ArchiveStore {
}

data Main {
}

machine Main::main(&mut self) {
}
