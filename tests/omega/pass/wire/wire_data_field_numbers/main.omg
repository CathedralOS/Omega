// Chapter 21 (Wire Protocols): plain `data` declarations may carry optional
// stable `#N` field identities. This fixture pins the accepted current schema
// syntax without the retired `wire data` declaration form.

data CounterMessage {
    #0 counter: i32;
    #1 timestamp_millis: i64;
}

data Main {
}

machine Main::main(&mut self) {
}
