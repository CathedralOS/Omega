machine build(builder: &mut Build) {
    builder.application("wire-compatibility-demand-report");
    builder.require_wire_compatibility<
        RollingChannel,
        MessageLineage,
        LocalMessage,
        PeerMessage,
        Readable,
        Writable,
        Canonical,
        CompleteMigration
    >();
}
