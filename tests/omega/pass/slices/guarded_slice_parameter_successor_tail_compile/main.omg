data Entry {
    value: i32;
}

data Main {
}

machine Main::main(&mut self) -> u64 {
    transition {
        _ -> 0
    }

    state choose(&mut self, entries: &[Entry], index: u64) -> u64 {
        transition entries.len >= index + 1 {
            true -> length(entries[index..])
            false -> 0
        }
    }

    state length(&mut self, entries: &[Entry]) -> u64 {
        transition {
            _ -> entries.len
        }
    }
}
