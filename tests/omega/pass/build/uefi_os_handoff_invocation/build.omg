machine build(builder: &mut Build) {
    builder.application("uefi-os-handoff-invocation");
    builder.subsystem = Subsystem::EfiApplication;
    builder.freestanding = true;
    builder.roots.bind(uefi_x86_64::ProgramEntry, Loader::run);
}
