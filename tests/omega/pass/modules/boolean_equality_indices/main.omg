use settings;
const ENABLED: bool = true;
data Flag<const Value: bool> { value: u8; }
machine keep(value: Flag<(ENABLED == true)>) -> Flag<true> {
    let local: Flag<(ENABLED != false)> = value;
    local
}
data Main {}
machine Main::main(&mut self) {}
