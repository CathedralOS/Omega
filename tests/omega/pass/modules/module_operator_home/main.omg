// Module-owned operator home: `units` declares the carrier-qualified
// `u64::Distance` domain and the `+` home `u64::Distance::add` on
// domain-constrained parameters. A broad `use units;` exposes the
// carrier-qualified spelling as a type constraint; the module-owned
// operator-home and domain-fact declarations must survive normalization,
// resolution, typing and checked semantics. The entry body is empty, so
// checked semantics is the witness.

use units;

data Main {}

machine measure(a: u64 in units::u64::Distance, b: u64 in units::u64::Distance) -> bool { true }

machine Main::main(&mut self) {
}
