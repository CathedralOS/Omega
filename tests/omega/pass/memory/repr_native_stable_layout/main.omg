// Chapter 20 "Stable Representation": a declaration may request a stable native
// layout with `repr native` so outside code can observe field layout. The parser
// accepts this provisional spelling and applies the compiler's native layout.

repr native
data WinHandle {
    value: u64;
}

data Main {
}

machine Main::main(&mut self) {
}
