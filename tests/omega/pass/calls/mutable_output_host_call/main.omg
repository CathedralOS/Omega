use omega_language_std::console;

data Main {
    console: Console;
    line: [u8; 64];
}

machine Main::main(&mut self) reaches Console {
    self.console.write("> ");
    _ = self.console.read_line(&mut self.line);
    self.console.write_line("read");
    self.console.exit_process(0);
}
