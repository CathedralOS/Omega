data Main {
    items: [i32; 4];
}

machine Main::main(&mut self, i: u64, j: u64, cut: u64) -> u64
    requires i < 2, j < 2, cut <= 4, i + j < 4, i + j < cut;
{
    let left: &mut [i32] = self.items[cut..4];
    self.items[i + j] = 7;
    left.len
}
