// Cross-target structural pin. Native confirmation remains a Linux-host task.

machine build(builder: &mut Build) {
    builder.application("cross_linux_time_host");
    builder.depend(Source::Path {
        location: "../../../../../source/library/std"
    });
}
