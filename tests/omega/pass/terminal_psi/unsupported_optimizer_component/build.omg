machine build(builder: &mut Build) {
    builder.application("unsupported_optimizer_component");
    builder.roots.bind(linux_x86_64::ProgramEntry, Main::main);
    builder.optimizations.enable(Optimization::ControlFlowCleanup);
}
