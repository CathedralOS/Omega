// A literal may qualify into a declared semantic domain when its value proves
// the domain predicate. Qualification is checked semantics and adds no runtime
// representation.
domain i64::Km
requires
    self >= 0;

machine mint_literal() {
    let distance: i64 in Km = 5 as i64 in Km;
}
