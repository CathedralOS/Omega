machine dive(remaining: u64, cap: u64)
terminates by remaining in 0..(cap % 5 + 6);
-> u64
requires remaining <= 5
{
    transition remaining > 0 {
        true -> dive(remaining - 1, cap - 0)
        false -> remaining
    }
}
