data Countdown {}
measure Countdown::Remaining(value: u64 [0..=5]) -> u64 [0..=5] { value }

machine walk(remaining: u64)
terminates by remaining -> Countdown::Remaining in 0..=5;
-> u64
requires remaining <= 5
{
    transition remaining > 0 {
        true -> walk(remaining - 1)
        false -> remaining
    }
}
