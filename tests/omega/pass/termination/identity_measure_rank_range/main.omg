data Countdown {}
measure Countdown::Remaining(value: u64) -> u64 { value }

machine walk(remaining: u64 [0..=5])
terminates by remaining -> Countdown::Remaining in 0..=5;
-> u64 {
    transition remaining > 0 {
        true -> walk(remaining - 1)
        false -> remaining
    }
}
