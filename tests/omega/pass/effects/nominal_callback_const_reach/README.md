# Nominal callback reach in constant evaluation

From the repository root:

```sh
cargo run -p omega -- --check tests/omega/pass/effects/nominal_callback_const_reach/main.omg
```

The nominal Step requirement permits Console, but the selected quiet callback
publishes empty reach. The same closed traversal used by ordinary calls must
therefore evaluate to the fixed-array length 7. Preparation must leave the
authored generic template intact for subsequent checking and package review.

The adjacent negative cases preserve a selected Console contribution and a
structural callback's fixed Console row respectively. Selection never removes
the requirement's suspension or blocking envelope.
