//! Optimizer module role: stage group. Callable record, manifest, and custody mutations.

mod custody;
mod fixture;
mod manifest_fields;
mod manifest_wire;
mod record_fields;
mod record_wire;
mod wire_offsets;
