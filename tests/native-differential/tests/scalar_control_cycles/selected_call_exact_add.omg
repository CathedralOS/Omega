machine identity(value: u64) -> u64 { value }

machine countdown(remaining: u64 [0..=5], marker: u64)
terminates by remaining -> Nat::Descending in 0..6;
-> u64 {
    transition remaining > 0 {
        true -> countdown(remaining - 1, identity(remaining + 1))
        false -> marker
    }
}
