# Delta internal boundary

Run `sh tests/delta/internal-boundary/run.sh` on macOS arm64 with `python3`
and `codesign`, or Windows x64 from Git Bash with `python3`. Other hosts fail
explicitly; Windows execution remains unvalidated in this change. The gate
materializes the role-selected Gamma evaluator and
the exact manifested Delta implementation with canonical and diagnostic
entrances. The diagnostic entrance is the three-member `controls.gamma.sources`
closure: `main.gamma` coordinates, `typing.gamma` owns retained typing controls,
and `metadata.gamma` owns emission metadata controls. No host parser extracts
or replaces production functions.

The private ordinary-Gamma entrance calls the actual `typing_resume` and
`publish_compiler_failure`. Unknown kinds above, below, and at zero outside
the closed 1–7 range report `InternalFailure` code 1
(`retained_frontend_contradiction`), space 3, with active top-row coordinates
0, 2, and 1. All three are exact 40-byte frames with status 3 and zero
limit/requested. These synthetic retained objects are not admitted Delta
source or a claim that source can construct compiler continuation frames.

Empty depth and a valid false-branch continuation complete unchanged. Nonpair
outer/retained fields and negative-depth unknown frames retain raw Gamma 249
with no output, rather than fabricated DCOUT. Eight controls run twice. Two
authored-source companions run through canonical DCREQ admission and retain
source Reject outcomes for an unexpected close and an unknown local. Every
observation compares the entire output, status, and empty stderr under the
unchanged 30-second watchdog.

Before the fix, the same three unknown-kind controls returned raw Gamma 249
and empty stdout/stderr; the other five controls already matched their current
expectations. The gate establishes this bounded internal branch and its
publisher, not all internal invariants or complete Delta boundary coverage.

## Emission metadata

Seventeen controls call actual `emit_advance`, Gamma node constructors,
`publish_checked_program`, and `publish_compiler_failure`. Negative cached
extents in a definition body, name, or parameter, and through nested calls and
lets, produce the exact 40-byte `InternalFailure` frame: code 2
(`emission_metadata_contradiction`), space 3, root row 0, zero limit/requested.
No application marker or receipt prefix may precede it. The body control uses
profile 1; the smaller neighboring publication controls use profile 0.

Direct negative running counts and amounts normalize to the sentinel `-1`,
including `INT64_MIN`. Zero and exact `INT64_MAX` counts remain valid. Positive
signed addition overflow still traps as raw Gamma 249 with empty stdout; this
gate does not claim a canonical DCOUT outcome for unrepresentable counts.

The fitting program publishes exactly `(def f () Int 0)` followed by two LFs:
one definition separator and the entry-owned final byte. A synthetic positive
body extent of 16,777,212 retains the resource-12 refusal, with requested
16,777,229 (14 prefix bytes, the body, and three closing/separator bytes).
These corrupt retained nodes are private controls, not authored Delta semantic
outcomes and not a complete metadata validator. All 25 private controls run
twice, followed by the two authored-source companions: 52 exact observations.
