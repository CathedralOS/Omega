; Scalar Functional Delta compiler discriminator.

(def sum_to ((n Int) (acc Int)) Int
  (if (eq n 0)
    acc
    (sum_to (- n 1) (+ acc n))))

(def main () Int
  (let start Int 5
    (sum_to start 0)))