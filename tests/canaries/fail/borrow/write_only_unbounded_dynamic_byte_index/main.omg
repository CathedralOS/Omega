// FAIL canary: write-only access does not bypass the ordinary index proof.

machine fill(bytes: &write [u8; 4], index: u64) {
    bytes[index] = 1;
}

data Main {
}

machine Main::main(&mut self) {
}
