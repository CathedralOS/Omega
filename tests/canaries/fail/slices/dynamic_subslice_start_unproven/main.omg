data Entry {
    value: i32;
}

data Main {
    entries: [Entry; 4];
    start: u64;
}

machine Main::main(&mut self) -> u64 {
    let view: &[Entry] = self.entries.as_slice();
    let tail: &[Entry] = view[self.start..];
    tail.len
}
