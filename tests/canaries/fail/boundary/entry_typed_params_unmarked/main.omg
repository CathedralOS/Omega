// FAIL: Main::run is an ordinary machine. Runtime entry selection is owned by
// an exact target ProgramEntry binding; the temporary corpus fallback recognizes
// only Main::main while that migration finishes.
data Main { }
machine Main::run(&self, handle: addr, table: addr) -> i32 {
    transition { _ -> (0) }
}
