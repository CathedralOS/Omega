domain [u8; 5]::Utf8
requires
    valid_utf8(self);

data Buffer
where
    used == storage.capacity,
{
    used: u64;
    storage: [u8; 5] in Utf8;
}

data Main {
}

machine Main::main(&mut self) {
    let buffer: Buffer = Buffer { used: 6, storage: "omega" };
}
