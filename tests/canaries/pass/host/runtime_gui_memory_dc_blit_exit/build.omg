// Windows memory-DC runtime canary: select the exact supported entry.
target windows_x64 { }

machine build(builder: &mut Build) {
    builder.application("runtime-gui-memory-dc-blit-exit");
    builder.roots.bind(windows_x86_64::ProgramEntry, Main::main);
}
