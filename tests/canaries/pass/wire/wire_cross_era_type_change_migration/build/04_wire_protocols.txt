# Omega Wire Protocols

wire data schemas: 1

## wire data CounterMessage
encoding: (default)
current era: 2
fields:
  0 counter f64
  1 timestamp_millis i64

### version v1 (era 0)
fields:
  0 counter i32
  1 timestamp_millis i64

### compatibility v1 -> v2
compatible:
  field 1 timestamp_millis i64 unchanged
requires migration:
  field 0 changes type i32 -> i64; decode via the old era's table and migrate up the chain
reserved:
  none
incompatible:
  none

### version v2 (era 1)
fields:
  0 counter i64
  1 timestamp_millis i64

### compatibility v2 -> current
compatible:
  field 1 timestamp_millis i64 unchanged
requires migration:
  field 0 changes type i64 -> f64; decode via the old era's table and migrate up the chain
reserved:
  none
incompatible:
  none
