use omega::language::core::float_operations;

// Runtime float formats are explicit. Mathematical Real is the proof-only
// carrier in omega::language::core::real, never a runtime literal format.
data Main {
}

machine Main::main(&mut self) {
    let machine_value: f64 = 3.0f64;
}
