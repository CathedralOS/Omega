machine build(builder: &mut Build) {
    builder.application("extent-root-provider-adapter");
    builder.accept_boundary<ExtentRootProvider>();
    builder.select_provider<ExtentRootProvider, ExtentRootProviderImpl>();
}
