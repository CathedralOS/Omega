// COMPILE CANARY: a value-returning entry reaches a nested state whose terminal
// value is an integer literal.
data Main {
}

machine Main::main(&mut self) -> i32 {
    transition { _ -> answer() }

    state answer() {
        25
    }
}
