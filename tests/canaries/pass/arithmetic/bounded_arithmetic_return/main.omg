data Main {
}

machine Main::main(
    &mut self,
    branch_id: u32 in Wrapping
) {
    transition {
        _ -> consume(branch_id + 1)
    }

    state consume(
        &mut self,
        amount: u32
    ) {
    }
}
