# Gamma compiler tests

| Retained child/files | Role | Deletion condition |
| --- | --- | --- |
| `test.sh` | Focused accepted/rejected compiler and resource-profile gate. | Reduce or delete only when checked source-to-tape validation subsumes each discriminator. |
| `validation/` | Independent reachable-tape structure checks. | Delete when the rooted certificate proves the same facts. |
