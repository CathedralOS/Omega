[package]
name = "omega-types"
edition.workspace = true
license.workspace = true
version.workspace = true

[lib]
name = "omega_types"
path = "src/lib.rs"

[dependencies]
omega-core = { path = "../omega-core" }
omega-resolve = { path = "../omega-resolve" }
