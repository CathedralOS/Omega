[package]
name = "omega-abstract-syntax-tree"
edition.workspace = true
license.workspace = true
version.workspace = true

[lib]
name = "omega_abstract_syntax_tree"
path = "src/lib.rs"

[dependencies]
omega-core = { path = "../../foundation/omega-core" }
