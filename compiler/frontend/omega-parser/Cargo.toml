[package]
name = "omega-parser"
edition.workspace = true
license.workspace = true
version.workspace = true

[lib]
name = "omega_parser"
path = "src/lib.rs"

[dependencies]
omega-abstract-syntax-tree = { path = "../omega-abstract-syntax-tree" }
omega-core = { path = "../../foundation/omega-core" }
omega-lexer = { path = "../omega-lexer" }
