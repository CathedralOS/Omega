[package]
name = "omega-source-files-to-tokens"
edition.workspace = true
license.workspace = true
version.workspace = true

[lib]
name = "omega_source_files_to_tokens"
path = "src/lib.rs"

[dependencies]
omega-core = { path = "../../foundation/omega-core" }
