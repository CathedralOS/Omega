; while-loop + let locals + assignment: sum 1..=n. sumto(10) = 55.
proc main() {
    return sumto(10)
}
proc sumto(n) {
    let total = 0
    let i = 1
    while i <= n {
        total = total + i
        i = i + 1
    }
    return total
}
