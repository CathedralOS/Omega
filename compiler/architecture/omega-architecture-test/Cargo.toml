[package]
name = "omega-architecture-test"
edition.workspace = true
license.workspace = true
version.workspace = true

# This crate exists only to host the architecture-layering guard test
# (see tests/layering.rs). It ships no library/binary of its own.
[lib]
name = "omega_architecture_test"
path = "src/lib.rs"

[dev-dependencies]
serde_json = "1.0"
