[package]
name = "omega-typed-trees"
edition.workspace = true
license.workspace = true
version.workspace = true

[lib]
name = "omega_typed_trees"
path = "src/lib.rs"

[dependencies]
psi-typed-trees = { path = "../../../psi-rs/representations/psi-typed-trees" }

[dev-dependencies]
# STR1 inventory pins (tests/semantic_taxonomy_inventory.rs) witness the
# representation-loss shapes across layers.
omega-core = { path = "../../foundation/omega-core" }
omega-effects = { path = "../omega-effects" }
omega-control-flow = { path = "../omega-control-flow" }
