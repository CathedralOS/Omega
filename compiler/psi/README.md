# Psi product source

This root is reserved for the eventual Omega-written, target-neutral Psi
implementation used by both the Delta-built bootstrap compiler and the
Omega-built optimizing compiler.

The current Rust implementation is explicitly transitional and lives at
`bootstrap/onramps/omega-rust/psi/`. Do not place new Rust crates here.
