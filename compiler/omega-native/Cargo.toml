[package]
name = "omega-native"
edition.workspace = true
license.workspace = true
version.workspace = true

[lib]
name = "omega_native"
path = "src/lib.rs"

[dependencies]
omega-core = { path = "../omega-core" }
omega-graph = { path = "../omega-graph" }
