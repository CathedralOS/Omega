[package]
name = "omega-machine-instructions"
edition.workspace = true
license.workspace = true
version.workspace = true

[lib]
name = "omega_machine_instructions"
path = "src/lib.rs"

[dependencies]
omega-control-flow = { path = "../omega-control-flow" }
omega-core = { path = "../../foundation/omega-core" }
omega-machine-program = { path = "../omega-machine-program" }
omega-target = { path = "../../backend/omega-target" }
