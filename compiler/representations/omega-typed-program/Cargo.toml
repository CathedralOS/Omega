[package]
name = "omega-typed-program"
edition.workspace = true
license.workspace = true
version.workspace = true

[lib]
name = "omega_typed_program"
path = "src/lib.rs"

[dependencies]
omega-core = { path = "../../foundation/omega-core" }
