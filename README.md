# Omega

Omega is a new programming language project implemented in Rust.

This repository starts as a small Rust workspace with:

- `omega/` for the core language crate
- `samples/` for example Omega programs
- `wiki/` for evolving language design notes

## Getting started

```bash
cargo test
```

## Layout

- `omega/`: language crate for syntax and compiler-facing building blocks
- `samples/`: small `.omega` example programs
- `wiki/`: working documentation for language ideas, semantics, and roadmap
