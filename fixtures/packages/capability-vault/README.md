# capability-vault

Capability-flow fixture. It is intended to produce distinct manifest rows for
stored, returned, acquired, and derived capability-bearing values.

Expected package evidence:

- capability-flow rows include store/return/acquire/derive cases;
- diff guidance points reviewers at authority retention and propagation paths.

