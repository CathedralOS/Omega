// The free-const SHADOWING WALK, field leg: bare field reads spell the
// field name, so a like-named free const collides; refused at the const.
use omega::language::std::console;
const flags: u32 = 3;
data Main { console: Console; flags: u32; }
machine Main::main(&mut self) { self.console.exit_process(0); }
