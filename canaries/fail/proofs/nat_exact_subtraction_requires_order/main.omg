// Bare Nat subtraction must not inherit the total monus behavior. Without an
// independent `used <= total` fact, its exact formation contract rejects.
use omega::language::core::nat;

machine remaining(total: Nat, used: Nat) -> Nat
{
    transition { _ -> (total - used) }
}
