machine main {
    pub entry(
        &mut self,
        label: &mut String
    ) {
        label = self.name();
    }

    entry name(
        &mut self
    ) -> String {
        -> "hello";
    }
}
