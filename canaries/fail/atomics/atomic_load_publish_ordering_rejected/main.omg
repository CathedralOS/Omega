// Atomic loads cannot publish prior writes, so a publish-bearing ordering has
// no valid meaning on this operation.

data Main { counter: AtomicU32; }

machine Main::main(&mut self) {
    let value: u32 = self.counter.load(Publish);
}
