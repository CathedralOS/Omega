data Main {
    speed: f32[finite, range<0.0f, 100000.0f>] = 0.0f;
}

machine Main::main(&mut self) {
    safe_speed(self.speed);


    state safe_speed(value: f32[finite, range<0.0f, 100.0f>]) {
    }
}
