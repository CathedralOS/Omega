boundary trait NativeSlice {
    machine send(value: &[u8]);
}

machine send(value: &[u8]) satisfies NativeSlice::send via Binding::DllImport("foreign", "send");

data Main { }
machine Main::main(&mut self) { }
