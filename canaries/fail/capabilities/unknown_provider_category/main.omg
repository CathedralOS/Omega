// Legacy registry diagnostic. The destination language rejects every top-level
// `provider` item rather than maintaining a fixed provider-category vocabulary.

provider omega::language::core::canary_bad : Teleportation;

data Main {
}

machine Main::main(&mut self) {
}
