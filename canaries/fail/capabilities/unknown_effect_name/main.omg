// Service rows resolve boundary-trait identities. `teleport_io` names no
// boundary trait, so the declaration is rejected.

boundary trait Console {
    machine write_line(text: &[u8])
    reaches
        teleport_io;
}

data Main {
    console: Console;
}

machine Main::main(&mut self) {
    self.console.write_line("hi");
}
