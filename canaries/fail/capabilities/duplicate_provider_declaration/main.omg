// Chapter 18 "Boundary Primitive Registry": providers are tracked records, not
// free-floating names. Declaring the same provider name twice is a duplicate
// registration and is rejected.

provider omega::language::core::canary_dup : Allocation;
provider omega::language::core::canary_dup : Allocation;

data Main {
}

machine Main::main(&mut self) {
}
