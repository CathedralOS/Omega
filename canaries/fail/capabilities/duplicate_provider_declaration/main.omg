// Legacy registry diagnostic. This canary disappears with the top-level
// `provider` item; duplicate satisfiers and provider-slot selections have their
// own nominal identity and coherence checks.

provider omega::language::core::canary_dup : Allocation;
provider omega::language::core::canary_dup : Allocation;

data Main {
}

machine Main::main(&mut self) {
}
