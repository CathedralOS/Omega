data Token {
    value: u64;
}

domain Token::Issued
established by TokenIssuer::issue;

// Naming a real requirement is insufficient: its exact result must guarantee
// the domain it is being authorized to establish.
trait TokenIssuer {
    machine issue(value: u64) -> Token
    ensures
        true;
}
