trait BaseReset {
    machine reset(&mut self) {
    }
}

trait ExplicitReset {
    requires BaseReset;
    machine reset(&mut self);
}

data Counter {
}

CounterExplicitReset: Counter satisfies ExplicitReset;
