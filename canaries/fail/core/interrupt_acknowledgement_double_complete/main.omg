use omega::language::core::interrupt;

data Main {}

machine Main::complete_twice(acknowledgement: InterruptAcknowledgement in Pending)
effects PortIo
{
    acknowledgement.complete();
    acknowledgement.complete();
}

machine Main::main(&mut self) {}
