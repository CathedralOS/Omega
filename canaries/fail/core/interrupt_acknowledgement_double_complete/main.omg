use omega::language::core::interrupt;

data Main {}

machine Main::complete_twice(acknowledgement: InterruptAcknowledgement)
effects PortIo
{
    acknowledgement.complete();
    acknowledgement.complete();
}

machine Main::main(&mut self) {}
