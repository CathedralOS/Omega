// Even through a mutable borrow of the lifecycle claim, another activation's
// parked continuation is not source-visible storage and cannot be mutated.
use omega::language::core::task;

data Main {}

machine Main::mutate(task: &mut Task<i32>) {
    task.continuation = 0;
}

machine Main::main(&mut self) {}
