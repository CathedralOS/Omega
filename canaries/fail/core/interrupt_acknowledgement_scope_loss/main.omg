use omega::language::core::interrupt;

data Main {}

machine Main::leak(acknowledgement: InterruptAcknowledgement in Pending) {
    // A forgotten EOI/acknowledgement is a linearity error.
}

machine Main::main(&mut self) {}
