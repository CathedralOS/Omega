// The subsystem word set is CLOSED: console, gui, efi_application.
target windows_x64 {
    subsystem fancy
}
boundary trait Console {
    machine exit_process(return_code: i32);
}
data Main {
    console: Console;
}
machine Main::main(&mut self) {
    self.console.exit_process(0);
}
