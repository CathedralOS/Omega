data Entry {
    value: i32;
}

data Main {
    entries: [Entry; 8];
}

machine Main::main(&mut self) -> u64 {
    let view: &[Entry] = self.entries.as_slice();
    let window: &[Entry] = view[2..5];
    let inner: &[Entry] = window[1..5];
    inner.len
}
