data Entry {
    value: i32;
}

data Main {
    entries: [Entry; 4];
}

machine Main::main(&mut self) -> i32 {
    let view: &[Entry] = self.entries.as_slice();
    let index: u64 = 9;
    let chosen: Entry = view[index];
    chosen.value
}
