data Entry {
    value: i32;
}

data Main {
    entries: [Entry; 8];
}

machine Main::main(&mut self) -> i32 {
    let view: &[Entry] = self.entries.as_slice();
    let window: &[Entry] = view[2..5];
    let chosen: Entry = window[3];
    chosen.value
}
