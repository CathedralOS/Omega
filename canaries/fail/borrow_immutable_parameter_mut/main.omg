data main {
    value: i32 = 1;
}

machine main(&mut self) {
    write(self.value);



    state copy(out_value: &mut i32) {
        out_value = 2;
    }
}
machine main::write(input_value: i32) {
    copy(mut input_value);
    
}
