machine main {
    owns value: i32 = 1;

    pub entry() {
        write(value);
    }

    fn write(input_value: i32) {
        copy(mut input_value);
    }

    state copy(out_value: &mut i32) {
        out_value = 2;
    }
}
