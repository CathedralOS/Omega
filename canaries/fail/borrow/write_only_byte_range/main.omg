// FAIL canary: byte-range projection needs normalized subrange loan and
// footprint identity, which this fixed-element milestone does not claim.

machine fill(bytes: &write [u8; 4]) {
    bytes[0..2] = [1, 2];
}

data Main {
}

machine Main::main(&mut self) {
}
