// FAIL canary: general aggregate projection remains outside the checked
// fixed-byte-array milestone.

data Pair {
    left: u8;
    right: u8;
}

machine fill(pair: &write Pair) {
    pair.left = 1;
}

data Main {
}

machine Main::main(&mut self) {
}
