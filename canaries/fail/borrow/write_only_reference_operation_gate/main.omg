// FAIL canary (write-only borrow representation rung): `&write T` has a
// source/typed identity, but no checked operation may use it until the
// non-observing operation lattice and attenuation rules are implemented.

machine fill(bytes: &write [u8]) {
}

data Main {
}

machine Main::main(&mut self) {
}
