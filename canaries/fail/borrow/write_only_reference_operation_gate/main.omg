// FAIL canary: the direct common-field rung does not recursively grant access
// through nested records.

data Inner {
    value: u8;
}

data Outer {
    inner: Inner;
}

machine fill(outer: &write Outer) {
    outer.inner.value = 1;
}

data Main {
}

machine Main::main(&mut self) {
}
