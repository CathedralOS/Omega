// FAIL canary: aggregates and projections remain outside the checked
// whole-scalar milestone.

machine fill(bytes: &write [u8; 4]) {
    bytes[0] = 1;
}

data Main {
}

machine Main::main(&mut self) {
}
