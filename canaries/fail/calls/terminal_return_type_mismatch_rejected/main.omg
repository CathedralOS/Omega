// Chapter 3: every reachable terminal path in a typed machine must produce a
// value compatible with the declared return type. Here the terminal expression
// is a String where the signature declares i32.
data Main {}

machine Main::main(&mut self) -> i32 {
    "hello"
}
