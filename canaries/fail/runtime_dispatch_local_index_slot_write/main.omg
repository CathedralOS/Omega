data Room {
    x: i32;
}

machine main {
    pub entry(
        &mut self
    ) {
        let rooms: [Room; 1] = [Room { x: 0 }];
        let index: Uint = 0;

        rooms[index].x = 1;

        -> entry() when true;
    }
}
