data Player {
    health: i32;
}

data Enemy {
    health: i32;
}

domain Enemy::Valid {
    self.health >= 0;
}

domain Player::Alive {
    self in Enemy::Valid;
    self.health > 0;
}

data Main {
    player: Player;
}

machine Main::main(&mut self) {
    self.player.health = 10;
}
