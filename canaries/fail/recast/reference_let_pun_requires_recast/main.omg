// A bare borrow retains its referent type. Exposing the same bytes through a
// different checked shape requires an explicit `as` recast.
data Bits {
    x: i64;
}

machine Bits::implicit_pun(&self) {
    let v: &f32 = &self.x;
}
