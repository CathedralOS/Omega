// Legacy registry rejection. The destination language rejects the `provider`
// item itself; package authorization instead governs satisfier admission and
// owned provider-slot selection.

provider app::game::fast_indexing : SliceIndexing;

data Main {
}

machine Main::main(&mut self) {
}
