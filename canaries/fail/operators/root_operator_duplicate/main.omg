operator Slice::index<T>(items: &[T], index: usize) -> T
requires
    index < items.len
intrinsic;

operator Slice::index<T>(items: &[T], index: usize) -> T
requires
    index < items.len
intrinsic;

data Main {
}

machine Main::main(&mut self) {
}
