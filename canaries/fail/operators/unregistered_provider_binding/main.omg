// Legacy registry rejection. The destination operator has no provider clause;
// missing or ambiguous selected satisfiers reject during provider-plan
// composition instead.

boundary operator [] Slice::index<T>(items: &[T], index: u64) -> T
provider omega::language::core::missing_provider
requires
    index < items.len;

data Main {
}

machine Main::main(&mut self) {
}
