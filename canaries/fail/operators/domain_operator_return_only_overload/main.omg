data Quantity {
    value: i32;
}

domain Quantity::Additive
requires
    self.value >= 0;

operator Quantity::Additive::combine(left: i32, right: i32) -> i32;
operator Quantity::Additive::combine(left: i32, right: i32) -> u64;

data Main {
}

machine Main::main(&mut self) {
}
