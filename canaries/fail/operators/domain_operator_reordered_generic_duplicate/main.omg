data Quantity {
    value: i32;
}

domain Quantity::Additive {
    self.value >= 0;

    operator combine<T, U>(left: T, right: U) -> T;
    operator combine<A, B>(left: B, right: A) -> B;
}

data Main {
}

machine Main::main(&mut self) {
}
