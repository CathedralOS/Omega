// FAIL CANARY. Omitted fields are zero-filled, and health=0 does not establish
// Player's default domain, so the zero-excluding witness is mandatory.
data Player
where
    health >= 1,
{
    health: i32;
    score: i32;
}
data Main {}
machine Main::main(&mut self) {
    let player: Player = Player {};
}
