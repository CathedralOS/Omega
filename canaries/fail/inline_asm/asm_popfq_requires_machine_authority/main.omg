// Restoring RFLAGS can change IF and other privileged machine state. Merely
// declaring MachineControl on a hosted root does not mint machine ownership.
use omega::language::core::assembly;

data Main {
    saved_flags: u64;
}

machine Main::main(&mut self) reaches MachineControl {
    asm { popfq self.saved_flags }
}
