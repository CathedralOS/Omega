// HLT itself changes no general-purpose register. Inventing rax in the block
// contract is rejected just like omitting a real clobber: the surface is exact.
data Main {}
machine Main::main(&mut self) effects machine_control {
    asm where clobbers rax { hlt }
}
