// The port source feeds DX and therefore requires exact u16 storage. A u32
// place cannot narrow implicitly merely because assembly consumes it.
use omega::language::core::assembly;

data Main { port: u32; status: u8; }
machine Main::main(&mut self) reaches PortIo {
    asm { in self.status, self.port }
}
