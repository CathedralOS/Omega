target linux_arm64 {
    host: omega::host::targets::linux {
        abi = syscall
        stdin = fd(0)
        stdout = fd(1)
        process = enabled
        filesystem = disabled
        threads = disabled
        clock = disabled
        random = disabled
    }

    trust omega::host::contracts
    trust omega::host::targets::linux
}

