data CounterSnapshot {
    value: i32;
}

trait BoundedCounter {
    invariant self.value in 0..=1000;

    machine increment(&mut self)
        ensures self.value in 0..=1000;
    machine snapshot(&self, out: &mut CounterSnapshot);
}

data Main {
}

machine Main::main(&mut self) {
}
