use omega::language::core::str;

data Main {
    text: String;
}

machine Main::main(&mut self) {
    self.text = "hello";

    let view: &str = self.text.as_view();
    self.text = "goodbye";

    let bytes: &[u8] = view.bytes();
}
