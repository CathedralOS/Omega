// Chapter 5 "Literals": real literals carry a suffix, e.g. `3.0real`.
// Float literal suffixes are not supported yet.
data Main {
}

machine Main::main(
    &mut self,
    out: &mut Real
) {
    let real_value: Real = 3.0real;
    out = real_value;
}
