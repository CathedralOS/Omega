// Chapter 5 "Literals": numeric literals are typed by suffix, e.g. `3i32`.
// Integer literal suffixes are not supported yet.
data Main {
}

machine Main::main(
    &mut self,
    out: &mut i32
) {
    let machine_value: i32 = 3i32;
    out = machine_value;
}
