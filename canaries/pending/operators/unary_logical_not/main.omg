// Chapter 5 "Operators" treats boolean logic as ordinary operations. The unary
// logical-not spelling `!a` is not yet parsed; the chapter examples sidestep it
// with `== false`.
data Main {
}

machine Main::main(
    &mut self,
    out: &mut bool
) {
    let flag: bool = true;
    out = !flag;
}
