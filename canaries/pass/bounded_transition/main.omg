machine main {
    owns health: i32[range<1, 100>] = 100;

    fn entry() {
        -> wounded(health) when health < 50;
        -> healthy();
    }

    state wounded(value: i32[range<1, 50>]) {
    }

    state healthy() {
    }
}
