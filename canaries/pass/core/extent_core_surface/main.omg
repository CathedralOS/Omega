use omega::language::core::extent;

// The concrete-range authority is source-visible but opaque. Its zero-usable
// container is an ordinary debt-free-first sum; only the Live path carries
// the conditional linear obligation.
data Holder {
    slot: ExtentSlot;
}

data Main {
}

machine Main::main(&mut self) {
}
