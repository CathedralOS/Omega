use omega::language::core::arena;

// Confirms the ALLOCATOR STORY STAGE 1 Arena surface is browsable: the core
// `arena` module declares the contract-only bounded-allocation boundary trait
// (allocation as an explicit capability — `allocate`/`deallocate` machines
// carrying the chapter 18 `alloc`/`dealloc` effect names), with no provider
// binding and no runtime. Importing it must type-check cleanly; nothing in
// stage 1 consumes it.

data Main {
}

machine Main::main(&mut self) {
}
