use omega::language::core::arena;

// Confirms the ALLOCATOR STORY STAGE 1 Arena surface is browsable: the core
// `arena` module declares the contract-only bounded-allocation boundary trait.
// It returns and reclaims the opaque linear Extent authority rather than a
// forgeable `addr`; `allocate`/`deallocate` both publish canonical `Arena`
// service reach. No provider or runtime is bound in stage 1.

data Main {
}

machine Main::main(&mut self) {
}
