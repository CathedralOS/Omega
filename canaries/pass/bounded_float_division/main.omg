data Main {
}

machine Main::main(
    &mut self
    ) -> Real[finite, range<0.0, 1.0>] {
    

    state answer(
        &mut self,
        raw: u32[wrapping]
    ) {
        -> Real::from(raw) / Real::from(u32::MAX);
    }
}
