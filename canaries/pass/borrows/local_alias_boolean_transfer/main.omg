data Main {
    value: i32;
}

machine Main::inspect(flag: bool)
requires
    flag
{
}

machine Main::main(&mut self)
requires
    self.value > 0
{
    let flag: bool = self.value > 0;
    self.inspect(flag);
}
