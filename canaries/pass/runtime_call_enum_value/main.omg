enum ItemKind {
    RustedDagger
    DeepKey
}

machine main(
    &mut self,
    out: &mut ItemKind
) {
    out = self.pick();
    
}

machine main::pick(
    &mut self
    ) -> ItemKind {
    -> ItemKind::DeepKey;
    
}
