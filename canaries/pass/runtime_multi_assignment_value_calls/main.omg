data Main {
}

machine Main::main(&mut self, flag: bool, out: &mut i32) {
    out = self.pick(flag) + self.pick(false);
    
}

machine Main::pick(&mut self, flag: bool) -> i32 {
    transition flag {
        true -> 1
        false -> 2
    }
    
}
