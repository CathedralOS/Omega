platform Console {
    fn write_line(text: String);
    fn exit_process(return_code: i32);
}

machine main {
    contains console: Console;

    owns return_code: i32 = 1;

    pub entry() {
        console.write_line("assign");
        return_code = 0;
        console.exit_process(return_code);
    }
}
