boundary trait Console {
    machine exit_process(return_code: i32);
}

data Main {
    console: Console;
    roll: i32;
}

machine Main::main(&mut self) {
    self.roll = 3;

    transition ((self.roll >= 5) == false) {
        true -> good()
        _ -> bad()
    }

    state good(&mut self) {
        self.console.exit_process(75);
    }

    state bad(&mut self) {
        self.console.exit_process(76);
    }
}
