// A recursive edge admitted by the explicit descending-natural ranking view.
machine countdown(remaining: u64)
terminates by remaining -> Nat::Descending;
{
    transition remaining > 0 {
        true -> countdown(remaining - 1)
        false -> done()
    }

    state done() {}
}
