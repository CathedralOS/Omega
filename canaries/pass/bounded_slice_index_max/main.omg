machine main -> usize[exact, range<1, 16>] {
    pub entry(
        room_count: usize[exact, range<1, 16>],
        rooms: [u32; 16],
        index: usize[exact, range<0, 15>]
    ) {
        max(room_count, index + 1)
    }
}
