invariant speed_range = [finite, range<0.0f, 100000.0f>];
machine main {
    owns speed: f32[speed_range] = 0.0f;

    pub entry() {
        clamp_speed(speed);
    }

    state clamp_speed(value: f32[speed_range]) {
    }
}
