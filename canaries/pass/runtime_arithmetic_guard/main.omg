data main {
    level: u32;
    xp: u32;
}

machine main {
    pub entry(&mut self) {
        self.level = 2;
        self.xp = 21;

        -> level_up() when self.xp >= 10 + self.level * 5;
        -> done();
    }

    state level_up(&mut self) {}

    state done(&mut self) {}
}
