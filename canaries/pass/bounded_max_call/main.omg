data Main {
}

machine Main::main(
    &mut self
    ) -> i32[exact, non_negative] {
    

    state answer(
        &mut self,
        amount: i32[exact, non_negative]
    ) {
        max(0, amount)
    }
}
