// Chapter 3: free-standing machines have no data receiver. `add_i32` is a math
// helper; the entry machine returns its result as the terminal value.
data Main {}

machine add_i32(left: i32, right: i32) -> i32 {
    left + right
}

machine Main::main(&mut self) -> i32 {
    add_i32(3, 4)
}
