enum ItemKind {
    RustedDagger
    DeepKey
}

data Main {
}

machine Main::main(
    &mut self,
    out: &mut ItemKind
) {
    out = self.pick();
    
}

machine Main::pick(
    &mut self
    ) -> ItemKind {
    ItemKind::DeepKey
}
