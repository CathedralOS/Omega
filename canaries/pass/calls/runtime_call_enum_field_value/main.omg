data ItemKind {
    case RustedDagger;
    case DeepKey;
}

data Reward {
    item: ItemKind
}

data Main {
}

machine Main::main(
    &mut self,
    reward: &mut Reward
) {
    reward.item = self.pick();
    
}

machine Main::pick(
    &mut self
    ) -> ItemKind {
    ItemKind::DeepKey
}
