data Reward {
    gold: i32;
}

data Random {
}

data Main {
    rng: Random;
}

machine Random::one(&mut self) -> i32 {
    1
}

machine Main::main(
    &mut self,
    reward: &mut Reward
) {
    reward.gold = 1 + self.rng.one();
}
