machine main {
    owns left: i32 = 1;
    owns right: i32 = 2;

    pub entry() {
        copy(mut left, right);
    }

    state copy(out_value: &mut i32, input_value: i32) {
        out_value = input_value;
    }
}
