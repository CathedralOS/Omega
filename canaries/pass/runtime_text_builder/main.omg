data Room {
    label: String;
}

data Line {
    text: String;
}

platform Console {
    fn write_line(text: String);
    fn exit_process(return_code: i32);
}

machine main {
    contains console: Console;

    owns room: Room;
    owns line: Line;

    fn entry() {
        room.label = "A1";
        line.text = "Room " + room.label;
        console.write_line(line.text);
        console.exit_process(0);
    }
}
