data Reward {
    gold: i32;
}

data Random {
}

data main {
    rng: Random;
}

machine Random::one(&mut self) -> i32 {
    -> 1;
}

machine main(
    &mut self,
    reward: &mut Reward
) {
    reward.gold = 1 + self.rng.one();
}
