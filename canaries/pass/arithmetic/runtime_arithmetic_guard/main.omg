data Main {
    level: u32;
    xp: u32;
}

machine Main::main(&mut self) {
    self.level = 2;
    self.xp = 21;

    transition {
        self.xp >= 10 + self.level * 5 -> level_up()
        _ -> done()
    }

    state level_up(&mut self) {}

    state done(&mut self) {}
}
