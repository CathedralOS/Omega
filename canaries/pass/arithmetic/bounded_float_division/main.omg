// Runtime floating point is an explicit finite representation. This ratio is
// a machine f64 computation; it does not pass through mathematical Real.
machine unit_ratio(raw: u32) -> f64 {
    (raw as f64) / (4294967295u32 as f64)
}

data Main {}

machine Main::main(&mut self) {
}
