// Chapter 13 "Generic Data": `Optional<T>` instantiated at a concrete type
// yields a concrete record layout after substituting the field type `T`.

data Optional<T> {
    has_value: bool;
    value: T;
}

data Main {
    maybe: Optional<i32>;
}

machine Main::main(&mut self) {
}
