data Exit {
    count: i32;
}

data Main {
}

machine Main::main(
    &mut self,
    exits: &mut [Exit],
    index: u64
)
requires
    index < exits.len
{
    exits[index].count = exits[index].count + 1;
}
