// A value may qualify into a declared semantic domain when its declared range
// entails every domain predicate. Qualification is checked semantics and adds
// no runtime representation.
domain i64::Km
requires
    self >= 0;

machine mint_range(raw: i64 [0..=4096]) {
    let distance: i64 in Km = raw as i64 in Km;
}
