machine main {
    pub entry(&mut self, flag: bool, out: &mut String) {
        out = self.pick(flag);
    }

    entry pick(&mut self, flag: bool) -> String {
        transition flag {
            true -> "Gate"
            false -> "Branch Room"
        }
    }
}
