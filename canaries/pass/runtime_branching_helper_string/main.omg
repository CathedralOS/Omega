machine main(&mut self, flag: bool, out: &mut String) {
    out = self.pick(flag);
    
}

machine main::pick(&mut self, flag: bool) -> String {
    transition flag {
        true -> "Gate"
        false -> "Branch Room"
    }
    
}
