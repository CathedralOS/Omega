data Main {
}

machine Main::main(&mut self, flag: bool, out: &mut String) {
    out = self.pick(flag);
    
}

machine Main::pick(&mut self, flag: bool) -> String {
    transition flag {
        true -> "Gate"
        false -> "Branch Room"
    }
    
}
