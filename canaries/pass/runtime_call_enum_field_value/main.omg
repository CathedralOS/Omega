enum ItemKind {
    RustedDagger
    DeepKey
}

data Reward {
    item: ItemKind
}

machine main(
    &mut self,
    reward: &mut Reward
) {
    reward.item = self.pick();
    
}

machine main::pick(
    &mut self
    ) -> ItemKind {
    -> ItemKind::DeepKey;
    
}
