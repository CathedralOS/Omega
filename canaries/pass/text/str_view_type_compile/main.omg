domain [u8]::Utf8 {
    valid_utf8(self);
}
use omega::language::core::str;

data Main {
    text: &[u8] in Utf8;
}

machine Main::main(&mut self) {
    let view: &[u8] in Utf8 = self.text.as_view();
}
