data Room {
    description: String;
}

data Main {
    room: Room;
}

machine Main::main(&mut self) {
    let room_alias: &mut Room = &mut self.room;
    room_alias.description = "prefix " + "omega";
}
