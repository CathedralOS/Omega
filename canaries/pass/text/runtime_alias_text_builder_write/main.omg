domain [u8]::Utf8
requires
    valid_utf8(self);
data Room {
    description: &[u8] in Utf8;
}

data Main {
    room: Room;
}

machine Main::main(&mut self) {
    let room_alias: &mut Room = &mut self.room;
    room_alias.description = "prefix " + "omega";
}