// PASS canary (decision 21, multiplicity rung 1): `[linear]` is a real
// declaration/type-parameter property and survives into the typed program.
// Obligation enforcement lands in the following checker rung.
data Receipt [linear] {}

data Envelope<T [linear]> [linear] {
    value: T;
}

data StoredReceipt [linear] {
    value: Envelope<Receipt>;
}

data Main {}

machine Main::main() -> i32 {
    0
}
