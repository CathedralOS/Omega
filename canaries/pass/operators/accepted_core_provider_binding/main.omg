// Legacy boundary-primitive registry coverage. This canary must migrate to an
// ordinary exact satisfier plus selected ProviderPlan, then be removed with the
// top-level `provider` item and operator-local provider clause.

provider omega::language::core::canary_allocation : Allocation;

boundary operator Arena::allocate(size: u64) -> addr
provider omega::language::core::canary_allocation
requires
    size > 0;

data Main {
}

machine Main::main(&mut self) {
}
