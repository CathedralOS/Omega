// Linux syscall provider canary: each supported target selects its exact entry.
target linux_x64 { }
target linux_arm64 { }

machine build(b: &mut Build) {
    b.roots.bind(linux_x86_64::ProgramEntry, Main::main);
    b.roots.bind(linux_arm64::ProgramEntry, Main::main);
}
