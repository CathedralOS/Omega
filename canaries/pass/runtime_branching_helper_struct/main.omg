enum RewardItem {
    None
    Gold
}

machine main(&mut self, flag: bool, out: &mut RewardItem) {
    out = self.pick(flag);
    
}

machine main::pick(&mut self, flag: bool) -> RewardItem {
    transition flag {
        true -> RewardItem::Gold
        false -> RewardItem::None
    }
    
}
