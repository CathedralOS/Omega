[package]
name = "trust-ledger"
edition.workspace = true
license.workspace = true
version.workspace = true

[lib]
name = "trust_ledger"
path = "src/lib.rs"

[dependencies]
trust-model = { path = "../trust-model" }
diagnostics = { path = "../../../psi/foundation/diagnostics" }

[dev-dependencies]
artifacts = { path = "../../tooling/artifacts" }
provider-planning = { path = "../provider-planning" }
compiler = { path = "../../compiler/compiler" }
effects = { path = "../../representations/effects" }
optimization-core = { path = "../../representations/optimization-core", features = ["test-support"] }
