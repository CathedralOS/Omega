//! Unit rows: continuations, scalars and the stores a unit body performs.

pub(in crate::installation_record) mod continuation;
pub(in crate::installation_record) mod scalar;
pub(in crate::installation_record) mod structural_scalar_field_store;
pub(in crate::installation_record) mod write_only_primitive_store;
