//! Scalar rows: the ABI, the call plan and the structural field store.

pub(in crate::installation_record) mod abi;
pub(in crate::installation_record) mod call_plan;
pub(in crate::installation_record) mod structural_scalar_field_store;
