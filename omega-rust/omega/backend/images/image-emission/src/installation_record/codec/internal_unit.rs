//! Internal unit calls: the call itself, its source and its scalar form.

pub(in crate::installation_record) mod call;
pub(in crate::installation_record) mod call_source;
pub(in crate::installation_record) mod scalar_call;
