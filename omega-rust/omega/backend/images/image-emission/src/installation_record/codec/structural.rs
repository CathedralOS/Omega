//! Structural rows: the argument, field, record, case, signature, source,
//! return and type shapes a structural value contributes to the record.

pub(in crate::installation_record) mod argument;
pub(in crate::installation_record) mod case;
pub(in crate::installation_record) mod field;
pub(in crate::installation_record) mod record;
pub(in crate::installation_record) mod returns;
pub(in crate::installation_record) mod scalar;
pub(in crate::installation_record) mod signature;
pub(in crate::installation_record) mod source;
pub(in crate::installation_record) mod types;
