//! End-to-end terminal-authority coverage for the settled portable filesystem
//! control and lifecycle policy
//! (`wiki/spec/build/permissions.md#portable-filesystem-control-and-lifecycle-authority`).
//!
//! The unit tests beside `native-realization` show that one retained
//! occurrence earns the evidence-bound explicit-empty mechanism row from a
//! hand-built replay record. This target drives the same production
//! mechanisms from an ordinary compile, so the board can see exactly how far
//! that route carries the evidence today.

#[path = "terminal_authority/filesystem_cohort_witness.rs"]
mod filesystem_cohort_witness;
#[path = "terminal_authority/filesystem_release_witness.rs"]
mod filesystem_release_witness;
