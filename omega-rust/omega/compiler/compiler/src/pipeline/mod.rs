//! Observations the compiler emits about a checked compilation: artifact
//! snapshots and derived reports. The stages themselves are the
//! source-files-to-assembled-syntax and assembled-syntax-to-checked-compilation
//! crates.

mod artifacts;
pub(crate) mod reporting;
