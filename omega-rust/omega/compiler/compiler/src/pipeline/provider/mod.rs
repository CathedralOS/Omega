//! Provider settlement: target machine declarations are filtered for the
//! requested targets, then provider plans are derived, validated, and
//! selected with provenance for the checked program.

pub(crate) mod selection;
pub(crate) mod target_machines;
