//! Public data declarations: `projection` projects the invariant facts of a
//! data definition and `evidence` requires the rederived definition facts
//! that back them.

pub(in crate::capture) mod evidence;
pub(in crate::capture) mod projection;
