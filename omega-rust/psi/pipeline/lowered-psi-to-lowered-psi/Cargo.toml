[package]
name = "lowered-psi-to-lowered-psi"
autotests = false
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
semantic-vocabulary = { path = "../../foundation/semantic-vocabulary" }
terminal-semantics = { path = "../../semantics/terminal-semantics" }
lowered-psi = { path = "../../representations/lowered-psi" }
optimization = { path = "../../representations/optimization" }
terminal-psi = { path = "../../representations/terminal-psi" }
terminal-codec = { path = "../../semantics/terminal-codec" }
terminal-verifier = { path = "../../semantics/terminal-verifier" }

[dev-dependencies]
language-core = { path = "../../foundation/language-core" }

[[test]]
name = "suite"
path = "tests/suite.rs"
