// Stable routed authority for one exact closed boundary requirement.
//
// `Binding<R>` is opaque and affine. Its values are established by build-time
// composition and installation, never by package-authored construction. A
// fused build may erase the carrier only after joining it to the exact selected
// provider for `R`; independent composition retains a runtime slot carrier.
//
// Service validity is intrinsic to the closed `Binding<R>` identity: the
// carrier already denotes toolchain establishment, so no authored domain
// qualification exists for it and any `Binding<R> in <domain>` spelling is
// rejected during source checking.

pub boundary data Binding<R>;
