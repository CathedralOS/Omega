// The `windows_x64` std target definition (file shape settled 2026-07-07:
// `std/targets/<target>.omg` = the target def; checked per-subsystem target
// implementations live beneath `std/targets/windows_x64/`). Target filtering
// keeps non-selected implementations inert.
use omega::language::std::targets::windows_x64::filesystem_impl;
