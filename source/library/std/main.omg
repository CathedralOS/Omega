// Ordinary package review entry for the portable standard-library surface.
// Target implementation modules are reached from their owning portable
// modules; target-only contracts remain separate until that target requests
// them explicitly.
use calling;
use console;
use filesystem;
use format_lineage;
use macos_gui;
use math;
use time;
use units;
use wire;
