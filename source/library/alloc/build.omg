// Product package: a bump allocator strategy over one qualified `Extent`
// (wiki/spec/resources/allocation.md, TASKS.md BUMP-ALLOCATOR-CANARY).

machine build(builder: &mut Build) {
    builder.package("alloc");
}
