; DATA-DEPENDENT loop with a <= guard: total += a for i=0..n  ->  (n+1)*a.
proc main() {
    let n = read_byte()
    let a = read_byte()
    let total = 0
    let i = 0
    state loop { to body when (i <= n)  return total }
    state body { total = total + a  i = i + 1  to loop }
}
