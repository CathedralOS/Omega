# Delta compiler work

The canonical Gamma-written Delta compiler and admitted tape remain absent.
`experiments/` retains the latest executable implementation material while the
Delta language shape is being measured. Nothing below that directory defines a
selected edge or amends [`../LANGUAGE.md`](../LANGUAGE.md).
