; CFG/state loop + let locals + assignment: sum 1..=n. sumto(10) = 55.
proc main() {
    return sumto(10)
}
proc sumto(n) {
    let total = 0
    let i = 1
    state loop { to body when (i <= n)  return total }
    state body { total = total + i  i = i + 1  to loop }
}
