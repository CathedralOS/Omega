[package]
name = "beta-lang"
version = "0.0.0"
edition = "2021"

[[bin]]
name = "beta-lang"
path = "src/main.rs"

# Empty workspace table => this is its own workspace root, so the parent Omega
# workspace never absorbs this throwaway on-ramp.
[workspace]
