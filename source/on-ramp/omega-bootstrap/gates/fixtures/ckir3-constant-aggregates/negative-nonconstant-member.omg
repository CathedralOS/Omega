data RuntimePair [copy] {
    first: u8;
    last: u8;
}

data RuntimeMemberProbe {
    seed: u8;
    pair: RuntimePair;
}

machine RuntimeMemberProbe::run(&mut self) -> u8 {
    self.seed = 35;
    self.pair = RuntimePair { first: self.seed, last: 70 };
    self.pair.last
}
