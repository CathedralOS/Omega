//! Placeholder library crate.
//!
//! The real content of `omega-architecture-test` is the integration test in
//! `tests/layering.rs`, which enforces the crate-dependency layering policy of
//! the Omega workspace. This `lib.rs` exists only so the crate has a build
//! target; it intentionally contains no logic.
