use data::dungeon_types;

platform machine ConsolePlatform {
    command Write(text: String);
    command WriteLine(text: String);
    command ReadLine(mut out_line: ConsoleLine);
    command ExitProcess(return_code: i32);
}
