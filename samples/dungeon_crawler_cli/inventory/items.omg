enum ItemKind {
    RustedDagger,
    CrackedBuckler,
    HealingCharm,
    EmberBlade,
    DeepKey,
}

data InventoryItem {
    kind: ItemKind;
    count: u32;
}

data Inventory {
    items: [InventoryItem; 32];
    item_count: usize;
    gold: u32;
}
