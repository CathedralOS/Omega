use enemies::enemies;
use rewards::rewards;

enum RoomEventKind {
    Nothing,
    FountainOfLife,
    Treasure,
    GoldCache,
    Enemy,
}

data RoomEvent {
    kind: RoomEventKind;
    enemy: Enemy;
    reward: Reward;
    consumed: bool;
}
