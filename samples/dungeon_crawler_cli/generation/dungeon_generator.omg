use generation::maze_builder;
use rooms::rooms;

machine DungeonGenerator {
    contains maze: MazeBuilder;

    fn generate(&mut self, seed: u64[wrapping], out_level: &mut Level) {
        self.maze.build(seed, out_level);
    }
}
