use generation::maze_builder;
use rooms::rooms;

data DungeonGenerator {
    maze: MazeBuilder;
}

machine for DungeonGenerator {
    pub entry generate(
        &mut self,
        seed: u64[wrapping],
        out_level: &mut Level
    ) {
        self.maze.build(seed, out_level);
    }
}
