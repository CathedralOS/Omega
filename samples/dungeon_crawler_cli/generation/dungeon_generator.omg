use generation::maze_builder;
use rooms::rooms;

machine DungeonGenerator {
    contains maze: MazeBuilder;

    fn generate(seed: u64[wrapping], out_level: &mut Level) {
        maze.build(seed, out_level);
    }
}
