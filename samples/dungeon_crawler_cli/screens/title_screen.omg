use platform::console;

machine TitleScreen {
    contains console: ConsolePlatform;

    state show() {
        console.write_line("Omega Dungeon");
        console.write_line("A tiny generated crawl for compiler stress.");
        console.write_line("Type look, exits, a direction, or quit.");
    }
}
