// Portable host sketch.
//
// `StandardHost` is the copy-paste sample target. It says "pick the default
// host ABI for this target triple" while still keeping the trust boundary
// visible in `build.omg`.

capability StandardHost {
    stdout: Stdout
    process: Process
}
