# Beta bootstrap tools

| Retained file | Role | Deletion condition |
| --- | --- | --- |
| `artifact_env.sh` | Materializes the canonical Beta assembler tape in the selected Alpha seed. | Delete when Alpha accepts raw tapes directly. |
| `build.sh` | Builds a requested Beta source into a disposable runnable container. | Delete when a cheaper canonical invocation replaces stamping. |
