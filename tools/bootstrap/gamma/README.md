# Gamma bootstrap tools

| Retained file | Role | Deletion condition |
| --- | --- | --- |
| `artifact_env.sh` | Materializes the canonical Gamma compiler tape. | Delete when Alpha accepts raw tapes directly. |
| `rebuild-artifact.sh` | Rebuilds or checks the canonical Gamma compiler tape from its Beta source. | Delete only when stronger exact construction replaces it. |
